# Sameer-s-fight Project Context

Last updated: 2026-02-20
Project root: `C:\Users\SAM\Downloads\Portfolio\sameer-s-fight-code-main`

This file is the local carry-forward context for the portfolio starter project only.

## Project purpose
- Portfolio website starter UI.

## Stack
- Vite 5
- React 18 + TypeScript
- Tailwind CSS
- shadcn/ui components

## Current status
- Dependencies are installed (`npm install` completed on 2026-02-20).
- Dev server was started and verified at `http://127.0.0.1:5173` (HTTP 200).

## Runbook
- Install deps: `npm install`
- Start dev: `npm run dev`
- Build: `npm run build`
- Lint: `npm run lint`
- Test: `npm test`

## Key paths
- `package.json`
- `src/`
- `public/`
- `vite.config.ts`
- `tailwind.config.ts`

## Working mode for future chats
- Open and work from this directory (`sameer-s-fight-code-main`) only.
- Keep edits scoped to this project.
- Run checks before handoff when making code changes.

## Useful local commands
- If `5173` is in use: `npm run dev -- --port 5174`
- Check active listener: `netstat -ano | findstr :5173`
